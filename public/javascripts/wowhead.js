jQuery(document).ready(function() {
/*	$('.wowhead').attach(WowheadHandler);*/
});

/*WowheadHandler = $.klass({
	onmouseover: function(event) {
		if (this.element.parent().children(".wowhead_tooltip").length > 0) {
			this.element.parent().children(".wowhead_tooltip").fadeIn();
		} else {
			url = this.element.attr("href");
			$link = this.element;
		
			$.ajax({
				type: "GET",
				url: url,
			
				success: function(data, textStatus) {
					$link.after(data);
				}
			});
		}
		
		return false;
	},
	
	onmouseout: function(event) {
		$(".wowhead_tooltip").fadeOut();
	}
	
	
});*/